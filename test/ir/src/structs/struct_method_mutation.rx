struct Counter {
    value: i32,
}

impl Counter {
    fn increment(mut self) -> i32 {
        self.value = self.value + 1;
        self.value
    }
}

fn main() {
    let mut c: Counter = Counter { value: 0 };
    printlnInt(c.increment());
    printlnInt(c.increment());
    exit(0);
}
