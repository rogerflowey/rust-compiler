struct Point {
    x: i32,
    y: i32,
}

impl Point {
    fn distance_from_origin(self) -> i32 {
        self.x + self.y
    }
}

fn main() {
    let p: Point = Point { x: 3, y: 4 };
    printlnInt(p.distance_from_origin());
    exit(0);
}
