fn main() {
    let mut n: i32 = getInt();
    let mut count: i32 = 0;
    while (n > 0) {
        if (n % 2 == 1) {
            count = count + 1;
        }
        n = n / 2;
    }
    printlnInt(count);
    exit(0);
}
