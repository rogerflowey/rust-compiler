fn factorial(n: i32) -> i32 {
    let mut acc: i32 = 1;
    let mut i: i32 = 1;
    while (i <= n) {
        acc = acc * i;
        i += 1;
    }
    acc
}

fn main() {
    let n: i32 = getInt();
    let result: i32 = factorial(n);
    printlnInt(result);
    exit(0);
}
